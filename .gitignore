# Virtual environment
venv/
.venv/
env/

# Model artifacts — large binary files, regenerated by serialize_model.py
models/*.joblib
models/*.pkl
models/*.onnx

# Prediction logs — runtime data, not source code
logs/

# Python cache
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python

# Jupyter checkpoints
.ipynb_checkpoints/
*.ipynb_checkpoints

# OS files
.DS_Store
Thumbs.db
desktop.ini

# Editor / IDE
.vscode/
.idea/
*.swp
*.swo

# Temp Office lock files
~$*.pptx
~$*.docx
~$*.xlsx

# Environment variables (never commit secrets)
.env
.env.*
secrets.yaml

# Dataset — 106MB, exceeds GitHub's 100MB file limit
# Download separately and place in project root before running serialize_model.py
credit_card_attrition_dataset_copino.csv

# Claude internal state
.claude/
